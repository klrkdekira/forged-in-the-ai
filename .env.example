# Copy to .env for docker compose. All optional; the LLM client abstraction
# (TODO.md Phase 0) is what actually reads these once it lands.
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=
HOST_DATA_DIR=/absolute/path/to/data
