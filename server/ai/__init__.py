"""GM agent: prompts, retrieval, tool definitions (Phase 4 fills this in).

For now, just the LLM client abstraction (ADR-0001, NFR-6): any
OpenAI-compatible chat-completions endpoint, configured by base URL, model,
and an optional key.
"""
