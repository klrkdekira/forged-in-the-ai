# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# drift-check scratch file (scripts/check-drift.mjs)
src/api/.schema.check.d.ts

# TanStack Router: regenerated from src/routes/ by the Vite plugin (dev/test)
# and by `tsr generate` (build script), so it never needs to be committed.
src/routeTree.gen.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
